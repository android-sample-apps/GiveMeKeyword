package com.mashup.data.repositories

import com.mashup.domain.repositories.DrawingRepository

class DrawingRepositoryImpl : DrawingRepository {
    override suspend fun uploadImage() {
        //TODO: Firebase 아미지 업로드
    }
}